version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-80}:80"
    restart: always
    environment:
      - NODE_ENV=${NODE_ENV:-production}
